# ============================================================
# db (Drizzle ORM) — environment variables
# Used for migrations (drizzle-kit) and at runtime. Copy to db/.env
# or set DATABASE_URL at the root .env (drizzle.config.ts loads ../.env).
# ============================================================

# Postgres connection string (Neon)
# Format: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=
